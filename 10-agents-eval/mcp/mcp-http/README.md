# 🌐 HTTP MCP Ticket Server (`mcp-http`)

HTTP-based MCP сервер для поиска тикетов поддержки через streamable-http транспорт, предназначенный для использования с `MultiServerMCPClient` и LangChain агентами.

## 📋 Описание

Этот сервер предоставляет тот же функционал поиска тикетов, что и stdio версия (`mcp-server-demo`), но работает через **streamable-http** транспорт, что позволяет использовать его как удаленный сервис.

### Основные возможности:

- 🔍 **Поиск тикетов** по различным критериям
- 🌐 **HTTP API** на порту 8000
- 🔗 **Интеграция с LangChain** через `MultiServerMCPClient`
- 📊 **База данных**: 50 образцов тикетов поддержки

### Критерии поиска:

- `user_id` - ID пользователя
- `status` - статус (open, closed, pending, in_progress)
- `priority` - приоритет (low, medium, high, critical)
- `category` - категория (authentication, billing, feature, technical, security)
- `keyword` - поиск по заголовку и описанию

## ⚡ Быстрый старт

### Вариант 1: Через Makefile (рекомендуется)

```bash
cd mcp/mcp-http

# Показать доступные команды
make help

# Установить зависимости
make install

# Запустить сервер
make run

# Проверить работу базы данных
make test

# Показать информацию о сервере
make info
```

### Вариант 2: Напрямую через uv

```bash
cd mcp/mcp-http

# Установить зависимости
uv sync

# Запустить сервер на порту 8000 (по умолчанию)
uv run server.py

# Или на другом порту
PORT=9000 uv run server.py
```

**Примечание**: FastMCP использует порт 8000 по умолчанию. Можно изменить через переменную `PORT`.

Сервер запустится на порту 8000 и будет доступен по адресу:
```
http://localhost:8000/mcp
```

### 2. Используйте с LangChain агентом

```python
from langchain_mcp_adapters.client import MultiServerMCPClient
from langchain_openai import ChatOpenAI
from langgraph.prebuilt import create_react_agent

# Подключение к HTTP MCP серверу
client = MultiServerMCPClient({
    "tickets-http": {
        "transport": "streamable_http",  # подчеркивание!
        "url": "http://localhost:8000/mcp",
    }
})

# Получение инструментов и создание агента
tools = await client.get_tools()
model = ChatOpenAI(model="gpt-4o-mini")
agent = create_react_agent(model, tools)

# Использование агента
response = await agent.ainvoke({
    "messages": [{
        "role": "user",
        "content": "Найди все критические тикеты по безопасности со статусом open"
    }]
})
```

## ⚠️ Важно: Различие в именовании транспорта

При работе с HTTP транспортом важно понимать разницу в именовании:

| Место использования | Значение | Пример |
|-------------------|----------|--------|
| **В коде сервера** (Python) | `"streamable-http"` | `mcp.run(transport="streamable-http")` |
| **В конфигурации клиента** | `"streamable_http"` | `"transport": "streamable_http"` |

Это особенность MCP SDK - в Python коде используется дефис, а в JSON/dict конфигурации - подчеркивание.

## 🛠️ Makefile команды

Проект включает `Makefile` для упрощения работы:

| Команда | Описание |
|---------|----------|
| `make help` | Показать все доступные команды |
| `make install` | Установить зависимости через uv |
| `make run` | Запустить HTTP MCP сервер |
| `make test` | Протестировать базу данных и поиск |
| `make info` | Показать информацию о сервере и статус БД |
| `make regenerate-data` | Пересоздать базу данных с образцами |
| `make clean` | Очистить кеш и временные файлы |

## 🔧 Структура проекта

```
mcp-http/
├── server.py         # HTTP MCP сервер
├── sample_data.py    # Генератор тестовых данных
├── pyproject.toml    # Конфигурация и зависимости проекта
├── Makefile          # Команды для управления проектом
├── data/
│   └── requests.xls  # База данных тикетов (50 образцов)
└── README.md         # Эта документация
```

## 📝 Примеры использования

### Поиск критических тикетов по безопасности

```python
response = await agent.ainvoke({
    "messages": [{
        "role": "user",
        "content": "Покажи все критические открытые тикеты по безопасности"
    }]
})
```

Агент использует инструмент `search_tickets` с параметрами:
```json
{
  "status": "open",
  "priority": "critical",
  "category": "security"
}
```

### Поиск тикетов конкретного пользователя

```python
response = await agent.ainvoke({
    "messages": [{
        "role": "user",
        "content": "Найди все тикеты пользователя user123"
    }]
})
```

### Комбинированный поиск

```python
response = await agent.ainvoke({
    "messages": [{
        "role": "user",
        "content": "Покажи высокоприоритетные проблемы с оплатой, которые еще открыты"
    }]
})
```

## 🆚 Отличия от stdio версии

| Характеристика | stdio версия | HTTP версия |
|---------------|--------------|-------------|
| **Транспорт** | stdio | streamable-http |
| **Порт** | N/A | 8000 |
| **URL** | N/A | http://localhost:8000/mcp |
| **Функция** | sync | **async** |
| **Запуск** | `uv run mcp dev main.py:mcp` | `uv run server.py` |
| **Тестирование** | MCP Inspector | HTTP клиент или агент |
| **Клиент** | `stdio_client` | `MultiServerMCPClient` |
| **Зависимости** | Включает LangChain клиент | Минимальные (только сервер) |

## 🔗 Интеграция с несколькими серверами

`MultiServerMCPClient` поддерживает подключение к нескольким MCP серверам одновременно:

```python
client = MultiServerMCPClient({
    "tickets-http": {
        "transport": "streamable_http",
        "url": "http://localhost:8000/mcp",
    },
    "weather": {
        "transport": "streamable_http",
        "url": "http://localhost:9000/mcp",
    },
    # Можно добавить stdio серверы
    "local-tools": {
        "transport": "stdio",
        "command": "python",
        "args": ["/path/to/local_server.py"]
    }
})

# Все инструменты доступны в одном агенте
tools = await client.get_tools()
```

## 🐛 Отладка

### Проверка доступности сервера

```bash
# Убедитесь что сервер запущен
curl http://localhost:8000/mcp

# Проверка логов сервера
# Сервер выводит информацию о запросах в консоль
```

### Настройка порта

```bash
# Запустить на другом порту
PORT=9000 uv run server.py

# Или через Makefile (редактируйте переменную в Makefile)
make run
```

### Типичные проблемы

**Проблема**: Сервер не запускается - порт занят

```bash
# Найти процесс на порту 8000
lsof -i:8000

# Остановить процесс
kill -9 <PID>

# Или использовать другой порт
PORT=9000 uv run server.py
```

**Проблема**: `transport` не найден в клиенте

✅ **Решение**: Используйте `"streamable_http"` (с подчеркиванием) в конфигурации клиента

## 📚 Полезные ссылки

- [LangChain MCP Documentation](https://docs.langchain.com/oss/python/langchain/mcp) - Официальная документация по интеграции MCP с LangChain
- [MCP Specification](https://modelcontextprotocol.io/) - Спецификация протокола MCP
- [langchain-mcp-adapters](https://github.com/langchain-ai/langchain-mcp-adapters) - GitHub репозиторий адаптеров
- [MCP Transports](https://modelcontextprotocol.io/docs/concepts/transports) - Документация по транспортам MCP

## 📊 Структура данных

База данных содержит 50 тикетов со следующим распределением:

- **Категории**: authentication, billing, feature, technical, security
- **Статусы**: open, closed, pending, in_progress, rejected
- **Приоритеты**: low, medium, high, critical
- **Пользователи**: 20 различных пользователей
- **Агенты**: support_agent1-4, dev_team, security_team, billing_team, tech_lead, qa_team

Каждый тикет содержит:
- `ticket_id` - уникальный идентификатор
- `user_id` - ID пользователя
- `title` - заголовок проблемы
- `description` - подробное описание
- `status` - текущий статус
- `priority` - уровень приоритета
- `category` - категория проблемы
- `created_date` - дата создания
- `updated_date` - дата последнего обновления
- `assigned_to` - назначенный агент/команда

## 🎯 Использование в продакшене

Для продакшен использования рекомендуется:

1. **Настроить CORS** и аутентификацию
2. **Использовать реальную базу данных** (PostgreSQL, MongoDB)
3. **Добавить rate limiting** для защиты от злоупотреблений
4. **Настроить логирование** с ротацией файлов
5. **Запускать через supervisor** или systemd для автоперезапуска
6. **Использовать обратный прокси** (nginx) для SSL

Пример запуска через uvicorn (для продакшена):

```bash
# В будущих версиях MCP может поддерживать uvicorn напрямую
uv run server.py
```

