# План разработки RAG-ассистента на базе LangChain

## 📊 Отчет о прогрессе

| Спринт | Итерация | Функционал | Статус | Дата |
|--------|----------|------------|--------|------|
| 1 | 1 | Базовый эхо-бот с конфигурацией | ✅ Завершено | 28.10.2025 |
| 1 | 2 | Интеграция с LLM | ✅ Завершено | 28.10.2025 |
| 1 | 3 | История диалогов | ✅ Завершено | 28.10.2025 |
| 1 | 4 | Финальная полировка | ✅ Завершено | 28.10.2025 |
| 2 | 5 | Индексация PDF с командами управления | ✅ Завершено | 07.11.2025 |
| 2 | 6 | Полноценный RAG с query transformation | ✅ Завершено | 07.11.2025 |
| 2 | 7 | Финальная полировка и документация | ✅ Завершено | 07.11.2025 |
| 3 | 8 | Рефакторинг RAG с отображением источников | ✅ Завершено | 12.11.2025 |
| 3 | 9 | Синтез тестовых датасетов | ✅ Завершено | 12.11.2025 |
| 3 | 10 | Система оценки качества через RAGAS | ✅ Завершено | 12.11.2025 |
| 3 | 11 | Финальная полировка и документация | ✅ Завершено | 12.11.2025 |
| 4 | 12 | Конфигурируемые embeddings (OpenAI + HuggingFace) | ✅ Завершено | 12.11.2025 |
| 4 | 13 | Hybrid Retrieval (Semantic + BM25) | ✅ Завершено | 12.11.2025 |
| 4 | 14 | Cross-Encoder Reranking | ✅ Завершено | 12.11.2025 |
| 4 | 15 | Финальная полировка и документация | ✅ Завершено | 12.11.2025 |
| 5 | 16 | Инструмент rag_search и упрощение RAG | ✅ Завершено | 19.11.2025 |
| 5 | 17 | Создание агента и интеграция | ✅ Завершено | 19.11.2025 |
| 5 | 18 | Тестирование, evaluation и финальная полировка | ✅ Завершено | 19.11.2025 |
| 6 | 19 | Создание MCP сервера mcp-bank-agent | ✅ Завершено | 20.11.2025 |
| 6 | 20 | Интеграция MCP инструментов с агентом | ✅ Завершено | 20.11.2025 |
| 6 | 21 | Тестирование и финальная полировка | ✅ Завершено | 20.11.2025 |
| 7 | 22 | Разработка инструмента deposit_income_calculator | ✅ Завершено | 20.11.2025 |
| 7 | 23 | Интеграция с агентом и обновление промптов | ✅ Завершено | 20.11.2025 |
| 7 | 24 | Документация и тестирование | ✅ Завершено | 20.11.2025 |
| 8 | 25 | MCP инструмент open_credit_card | ✅ Завершено | 26.11.2025 |
| 8 | 26 | Интеграция HITL в агента | ✅ Завершено | 27.11.2025 |
| 8 | 27 | UI и обработка HITL в боте | ✅ Завершено | 27.11.2025 |
| 8 | 28 | Тестирование и документация | ✅ Завершено | 27.11.2025 |
| 9 | 29 | Модуль `middleware.py` и `PIIMiddleware` | ✅ Завершено | 25.05.2026 |
| 9 | 30 | Подключение PII-слоя в `src/agent.py` | ✅ Завершено | 25.05.2026 |
| 9 | 31 | Документация (idea, vision, conventions, README) | ✅ Завершено | 25.05.2026 |
| 9 | 32 | Сквозное тестирование PII + HITL | ✅ Завершено | 25.05.2026 |
| 10 | 33 | `ModelCallLimitMiddleware` и `ToolCallLimitMiddleware` | ✅ Завершено | 25.05.2026 |
| 10 | 34 | Интеграция лимитов в `src/agent.py` | ✅ Завершено | 25.05.2026 |
| 10 | 35 | Документация call limits | ✅ Завершено | 25.05.2026 |
| 10 | 36 | Сквозное тестирование лимитов | ✅ Завершено | 25.05.2026 |
| 11 | 37 | MCP инструмент open_deposit | ✅ Завершено | 25.05.2026 |
| 11 | 38 | Интеграция HITL для open_deposit | ✅ Завершено | 25.05.2026 |
| 11 | 39 | Обновление промпта и документации | ✅ Завершено | 25.05.2026 |
| 11 | 40 | Сквозное тестирование open_deposit | ✅ Завершено | 25.05.2026 |

**Легенда статусов:**
- ⏳ Не начато
- 🚧 В работе
- ✅ Завершено
- ❌ Заблокировано

---

## 🚀 Спринт 1: Базовый LLM-бот (Завершено)

### Итерация 1: Базовый эхо-бот с конфигурацией

**Цель:** Запустить простейшего Telegram бота с конфигурацией через .env

- [x] Создать `pyproject.toml` с зависимостями: aiogram, openai, python-dotenv
- [x] Создать структуру папок: `src/`
- [x] Создать `.env.example` с шаблоном переменных
- [x] Создать `.gitignore` (`.env`, `__pycache__`, `.venv`)
- [x] Создать `Makefile` с командами: `install`, `run`
- [x] Создать базовый `README.md` с инструкцией по запуску
- [x] Создать `src/config.py` с классом Config и загрузкой из .env
- [x] Создать `src/bot.py` с инициализацией Bot и Dispatcher
- [x] Создать `src/handlers.py` с обработчиками `/start` и эхо-ответов
- [x] Добавить базовое логирование (старт бота, входящие сообщения)

**Как протестировать:**
- `uv sync` устанавливает зависимости
- Создать `.env` файл с TELEGRAM_TOKEN
- Запустить бота: `make run`
- Отправить `/start` → получить приветствие
- Отправить любой текст → получить его же обратно

---

### Итерация 2: Интеграция с LLM

**Цель:** Заменить эхо на реальные ответы от LLM

- [x] Создать `src/llm.py` с функцией `get_response(messages: list) -> str`
- [x] Инициализировать AsyncOpenAI клиент с настройками из config
- [x] Обработать ошибки при вызове LLM (try/except)
- [x] Обновить обработчик сообщений: отправлять запрос в LLM
- [x] Пока без истории - отправлять только текущее сообщение

**Как протестировать:**
- Отправить вопрос боту
- Получить осмысленный ответ от LLM
- Проверить, что ошибки сети обрабатываются корректно

---

### Итерация 3: История диалогов

**Цель:** Добавить контекст диалога, чтобы LLM помнил предыдущие сообщения

- [x] Создать глобальный словарь `chat_conversations: dict[int, list[dict]]`
- [x] При `/start` инициализировать историю с системным промптом
- [x] При новом сообщении добавлять его в историю чата
- [x] Передавать всю историю в `llm.get_response()`
- [x] Сохранять ответ LLM в историю

**Как протестировать:**
- Начать диалог: "Меня зовут Иван"
- Спросить: "Как меня зовут?"
- LLM должен ответить "Иван"
- Отправить `/start` и повторить вопрос - LLM не должен помнить имя

---

### Итерация 4: Финальная полировка

**Цель:** Довести бота до production-ready состояния

- [x] Улучшить сообщения об ошибках для пользователя
- [x] Добавить обработку edge cases (пустые сообщения, длинные тексты)
- [x] Проверить и дополнить README.md
- [x] Добавить примеры использования в README
- [x] Финальное тестирование всех сценариев

**Как протестировать:**
- Пройти все сценарии из docs/vision.md раздел "Сценарии работы"
- Проверить работу с несколькими пользователями одновременно
- Проверить корректность логов

---

## 🚀 Спринт 2: RAG-ассистент на базе LangChain

### Итерация 5: Индексация PDF с командами управления

**Цель:** Загрузить PDF-документы, разбить на чанки, создать векторное хранилище и добавить команды управления индексацией

- [x] Добавить зависимости в `pyproject.toml`: langchain, langchain-openai, langchain-community, langchain-core, langchain-text-splitters, pypdf
- [x] Обновить `config.py` - добавить MODEL_QUERY_TRANSFORM, EMBEDDING_MODEL, DATA_DIR
- [x] Создать `src/indexer.py` с функциями:
  - [x] `load_pdf_documents(data_dir)` - загрузка PDF через PyPDFLoader
  - [x] `split_documents(pages)` - разбиение через RecursiveCharacterTextSplitter (chunk_size=500)
  - [x] `create_vector_store(chunks)` - создание InMemoryVectorStore
  - [x] `reindex_all()` - асинхронная функция полной переиндексации
- [x] Создать `src/rag.py` с глобальной переменной `vector_store` и retriever
- [x] Обновить `bot.py` - вызвать индексацию при старте
- [x] Добавить в `handlers.py` команды `/index` и `/index_status`
- [x] Добавить логирование процесса индексации

**Как протестировать:**
- Запустить бота - должна пройти индексация PDF
- Проверить логи индексации
- Выполнить `/index_status` - увидеть количество документов
- Выполнить `/index` - переиндексировать документы

---

### Итерация 6: Полноценный RAG с query transformation и историей

**Цель:** Реализовать полную RAG-цепочку из референсного ноутбука с поддержкой истории диалога и уточняющих вопросов

- [x] Реализовать в `src/rag.py`:
  - [x] `format_chunks(chunks)` - форматирование чанков с метаданными
  - [x] Промпты в отдельных файлах (prompts/conversation_system.txt, prompts/query_transform.txt)
  - [x] conversational_answering_prompt - ChatPromptTemplate для диалога
  - [x] retrieval_query_transform_prompt - промпт для трансформации запроса
  - [x] retrieval_query_transformation_chain - цепочка трансформации
  - [x] rag_query_transform_chain - финальная RAG-цепочка
  - [x] `rag_answer(messages)` - асинхронная функция для получения ответа
- [x] Обновить `handlers.py`:
  - [x] Хранение истории сразу в LangChain messages (HumanMessage, AIMessage, SystemMessage)
  - [x] Вызывать `rag_answer()` для всех сообщений
  - [x] Убрана проверка длины сообщения
- [x] Интегрировать с существующей системой истории диалогов
- [x] Удалить неиспользуемый модуль llm.py

**Как протестировать:**
- Задать вопрос: "Какие условия кредита?" - получить ответ на основе документов
- Диалог с уточнением: "Какие вклады есть?" → "А проценты какие?"
- Вопрос вне контекста: "Какая погода?" - получить "Я не нашел ответа"
- Проверить, что история сохраняется и контекст учитывается

---

### Итерация 7: Финальная полировка и документация

**Цель:** Обработка edge cases, добавление команды /help, логирование в файл, финальная документация

- [x] Обработка ошибок:
  - [x] Валидация наличия PDF-файлов в data/
  - [x] Проверка инициализации векторного хранилища
  - [x] Корректная обработка ошибок индексации с try/except
  - [x] Обработка ошибок при вызове RAG с откатом истории
- [x] Добавить команду `/help` с описанием всех возможностей бота
- [x] Настроить логирование в файл:
  - [x] Добавить FileHandler в logging для записи в файл logs/bot.log
  - [x] Логировать все важные события: старт/стоп, индексация, вопросы, ошибки
- [x] Обновить README.md:
  - [x] Описание RAG-функционала
  - [x] Инструкции по добавлению документов
  - [x] Примеры использования
  - [x] Описание всех команд
  - [x] Примеры конфигурации для OpenRouter и Fireworks
- [x] Улучшить архитектуру:
  - [x] Вынести инициализацию retriever в rag.py
  - [x] Добавить RETRIEVER_K в конфиг

**Как протестировать:**
- Проверить команду `/help`
- Протестировать все edge cases
- Убедиться, что README актуален
- Пройти полный сценарий использования

---

---

## 🚀 Спринт 3: Мониторинг и оценка качества RAG

### Итерация 8: Рефакторинг RAG с отображением источников

**Цель:** Модернизировать RAG цепочку для возврата источников и добавить LangSmith трейсинг

- [x] Обновить `src/rag.py`:
  - [x] Изменить `get_rag_chain()` для возврата dict с `answer` и `documents`
  - [x] Добавить функцию `format_sources(documents)` для форматирования списка источников
  - [x] Обновить `rag_answer()` для работы с новой структурой
- [x] Обновить `src/handlers.py`:
  - [x] Модифицировать обработчик сообщений для работы с `answer` и `documents`
  - [x] Добавить форматирование и отображение источников если `SHOW_SOURCES=true`
  - [x] Форматировать компактно: "📚 Источники: file.pdf (стр. 3, 5)"
- [x] Обновить `src/config.py`:
  - [x] Добавить `SHOW_SOURCES` (default: False)
  - [x] Добавить `LANGSMITH_API_KEY`, `LANGSMITH_TRACING_V2`, `LANGSMITH_PROJECT`, `LANGSMITH_DATASET`
- [x] Обновить `env.example`:
  - [x] Добавить секцию LangSmith configuration
  - [x] Добавить `SHOW_SOURCES=false`
- [x] Добавить зависимость `langsmith` в `pyproject.toml`

**Как протестировать:**
- Запустить бота с `SHOW_SOURCES=false` - источники не показываются
- Установить `SHOW_SOURCES=true` - внизу ответов появляются источники
- Проверить что источники форматируются компактно с группировкой страниц

---

### Итерация 9: Синтез тестовых датасетов

**Цель:** Создать модуль для автоматического синтеза датасетов из документов

- [x] Создать директорию `datasets/`
- [x] Создать `src/dataset_synthesizer.py`:
  - [x] `load_and_sample_documents(data_dir, samples_per_file=2)` - загрузка и выборка чанков
  - [x] `synthesize_qa_pairs(chunks, llm_model)` - генерация QA пар через LLM
  - [x] `load_json_qa_pairs(data_dir, samples_per_file=2)` - загрузка готовых Q&A из JSON
  - [x] `save_dataset(qa_pairs, filepath)` - сохранение в JSON
  - [x] `upload_to_langsmith(dataset_path, dataset_name)` - загрузка с проверкой дубликатов
  - [x] `main()` - CLI интерфейс с аргументом `--upload`
- [x] Обновить `Makefile`:
  - [x] Добавить команду `dataset` для создания датасета
  - [x] Добавить команду `dataset-upload` для загрузки в LangSmith
- [x] Обновить `.gitignore` - игнорировать `datasets/*.json`

**Как протестировать:**
- Выполнить `make dataset` - создается `datasets/06-rag-qa-dataset.json`
- Проверить формат датасета (по 2 QA из PDF + по 2 из JSON)
- Выполнить `make dataset-upload` - датасет загружается в LangSmith
- Повторить `make dataset-upload` - не создается дубликат

---

### Итерация 10: Система оценки качества через RAGAS

**Цель:** Реализовать evaluation RAG системы с метриками RAGAS и интеграцией с LangSmith

- [x] Создать `src/evaluation.py`:
  - [x] `check_dataset_exists(dataset_name)` - проверка через LangSmith API
  - [x] `init_ragas_metrics()` - инициализация метрик (один раз)
  - [x] `evaluate_dataset(dataset_name)` - главная функция evaluation (подход из референса раздел 5)
    - [x] Шаг 1: Запуск эксперимента с blocking=False и сбор данных
    - [x] Шаг 2: RAGAS batch evaluation
    - [x] Шаг 3: Загрузка метрик как feedback в LangSmith
- [x] Инициализация RAGAS метрик (6 штук):
  - [x] Faithfulness
  - [x] ResponseRelevancy (answer_relevancy)
  - [x] AnswerCorrectness
  - [x] AnswerSimilarity
  - [x] ContextRecall
  - [x] ContextPrecision
- [x] Добавить зависимости в `pyproject.toml`: `ragas>=0.2.0`, `datasets>=3.0.0`
- [x] Обновить `src/handlers.py`:
  - [x] Добавить команду `/evaluate_dataset [name]`
  - [x] Показывать прогресс evaluation
  - [x] Отображать результаты метрик с эмодзи и описаниями

**Как протестировать:**
- Убедиться что датасет загружен в LangSmith (через `make dataset-upload`)
- Запустить `/evaluate_dataset` без аргументов - использует LANGSMITH_DATASET из конфига
- Запустить `/evaluate_dataset custom-dataset` - использует указанный датасет
- Проверить вывод метрик: 6 RAGAS метрик с цветовыми индикаторами
- Проверить в LangSmith UI что feedback загружен к runs

---

### Итерация 11: Финальная полировка и документация

**Цель:** Тестирование, обработка ошибок, обновление документации

- [x] Обработка ошибок:
  - [x] Валидация LANGSMITH_API_KEY при использовании evaluation
  - [x] Проверка доступности LangSmith API
  - [x] Обработка ошибок RAGAS (timeout, rate limits)
  - [x] Понятные сообщения пользователю при ошибках
  - [x] Проверка существования датасета перед evaluation
- [x] Обновить `README.md`:
  - [x] Описание режима отображения источников
  - [x] Инструкции по настройке LangSmith
  - [x] Команда `/evaluate_dataset`
  - [x] Команды `make dataset` и `make dataset-upload`
  - [x] Описание RAGAS метрик
  - [x] Обновлена структура проекта
  - [x] Добавлен технологический стек
- [x] Финальное тестирование:
  - [x] Работа RAG с источниками (вкл/выкл)
  - [x] Трейсинг в LangSmith UI
  - [x] Создание датасета
  - [x] Загрузка датасета
  - [x] Evaluation с отображением результатов
  - [x] Feedback в LangSmith
- [x] Логирование:
  - [x] Логировать начало/конец evaluation
  - [x] Логировать количество обработанных примеров
  - [x] Логировать ошибки при evaluation
- [x] Конфигурация:
  - [x] Вынесены RAGAS модели в config (RAGAS_LLM_MODEL, RAGAS_EMBEDDING_MODEL)

**Как протестировать:**
- Пройти полный цикл: создание датасета → загрузка → evaluation
- Проверить все edge cases (нет API ключа, несуществующий датасет, ошибки сети)
- Убедиться что README актуален и содержит все новые функции
- Проверить что логи информативны

---

---

## 🚀 Спринт 4: Advanced Hybrid RAG

### Итерация 12: Конфигурируемые embeddings (OpenAI + HuggingFace)

**Цель:** Добавить поддержку локальных HuggingFace embeddings как альтернативу OpenAI

- [x] Добавить зависимости: `langchain-huggingface`, `sentence-transformers`
- [x] Обновить `config.py`:
  - [x] `EMBEDDING_PROVIDER` (openai/huggingface, default: openai)
  - [x] `HUGGINGFACE_EMBEDDING_MODEL` (default: intfloat/multilingual-e5-base)
  - [x] `HUGGINGFACE_DEVICE` (cpu/cuda/mps, default: cpu)
- [x] Обновить `indexer.py`:
  - [x] `create_embeddings()` - фабрика для создания embeddings по провайдеру
  - [x] Обновить `create_vector_store()` использовать `create_embeddings()`
- [x] Обновить `env.example` - добавить секцию Embeddings Configuration
- [x] Тестирование:
  - [x] С OpenAI embeddings (текущее поведение)
  - [x] С HuggingFace embeddings (локальные)

**Как протестировать:**
- Запустить с `EMBEDDING_PROVIDER=openai` - работает как раньше
- Запустить с `EMBEDDING_PROVIDER=huggingface` - использует локальные embeddings
- Проверить что индексация и поиск работают в обоих режимах

---

### Итерация 13: Hybrid Retrieval (Semantic + BM25)

**Цель:** Реализовать гибридный поиск с комбинацией semantic и BM25

- [x] Добавить зависимость: `rank-bm25`, `langchain-classic`
- [x] Обновить `config.py`:
  - [x] `RETRIEVAL_MODE` (semantic/hybrid/hybrid_reranker, default: semantic)
  - [x] `SEMANTIC_RETRIEVER_K` (default: 10)
  - [x] `BM25_RETRIEVER_K` (default: 10)
  - [x] `ENSEMBLE_SEMANTIC_WEIGHT` (default: 0.5)
  - [x] `ENSEMBLE_BM25_WEIGHT` (default: 0.5)
- [x] Обновить `rag.py`:
  - [x] Глобальная переменная `chunks` для BM25
  - [x] `create_semantic_retriever()` - создание semantic retriever
  - [x] `create_bm25_retriever()` - создание BM25 retriever
  - [x] `create_hybrid_retriever()` - EnsembleRetriever (semantic + BM25)
  - [x] `create_retriever()` - фабрика по режиму (semantic/hybrid)
  - [x] Обновить `initialize_retriever()` использовать фабрику
- [x] Обновить `indexer.py`:
  - [x] `reindex_all()` возвращает (vector_store, chunks) для BM25
- [x] Обновить `bot.py`:
  - [x] Сохранять chunks при индексации в `rag.chunks`
- [x] Обновить `env.example`

**Как протестировать:**
- `RETRIEVAL_MODE=semantic` - работает как раньше
- `RETRIEVAL_MODE=hybrid` - использует Semantic + BM25
- Задать вопрос с точным термином - hybrid должен лучше найти
- Проверить `/index_status` показывает режим

---

### Итерация 14: Cross-Encoder Reranking

**Цель:** Добавить cross-encoder для переранжирования результатов hybrid retrieval

- [x] Зависимость `sentence-transformers` уже есть (из итерации 12)
- [x] Обновить `config.py`:
  - [x] `CROSS_ENCODER_MODEL` (default: cross-encoder/mmarco-mMiniLMv2-L12-H384-v1)
  - [x] `RERANKER_TOP_K` (default: 3)
- [x] Обновить `rag.py`:
  - [x] Глобальная переменная `cross_encoder` (lazy loading)
  - [x] `get_cross_encoder()` - ленивая инициализация cross-encoder
  - [x] `rerank_documents(query, documents, top_k)` - переранжирование
  - [x] Обновить `create_retriever()` для режима hybrid_reranker
- [x] Обновить `get_rag_chain()`:
  - [x] Для hybrid_reranker: ensemble_docs → rerank → documents → answer
  - [x] LCEL цепочка с промежуточным шагом reranking
- [x] Обновить `env.example`

**Как протестировать:**
- `RETRIEVAL_MODE=hybrid_reranker` - включает reranking
- Сравнить ответы между hybrid и hybrid_reranker на одном вопросе
- Проверить что возвращается RERANKER_TOP_K документов
- Логи должны показывать загрузку cross-encoder при первом использовании

---

### Итерация 15: Финальная полировка и документация

**Цель:** Тестирование всех режимов, обработка ошибок, обновление документации

- [x] Обработка ошибок:
  - [x] Валидация RETRIEVAL_MODE (только разрешенные значения)
  - [x] Валидация EMBEDDING_PROVIDER
  - [x] Обработка ошибок загрузки моделей HuggingFace
  - [x] Обработка ошибок инициализации cross-encoder
  - [x] Информативные логи при переключении режимов
- [x] Улучшения команд:
  - [x] `/index_status` показывает: retrieval_mode, embedding_provider, модели
  - [x] `/help` обновить с описанием новых режимов
- [x] Обновить `README.md`:
  - [x] Описание трех режимов retrieval
  - [x] Таблица с рекомендациями когда использовать каждый режим
  - [x] Инструкции по настройке HuggingFace embeddings
  - [x] Примеры конфигурации для каждого режима
  - [x] Требования к ресурсам (CPU/GPU, память)
  - [x] Обновленный раздел Configuration
- [x] Финальное тестирование:
  - [x] Все три режима retrieval
  - [x] Оба провайдера embeddings (OpenAI, HuggingFace)
  - [x] Переключение между режимами
  - [x] Evaluation на датасете для сравнения метрик
- [x] Логирование:
  - [x] При старте логировать: режим, провайдер, модели
  - [x] Логи инициализации каждого компонента
- [x] Дополнительно:
  - [x] Добавлены метрики ContextRecall и ContextPrecision (всего 6 RAGAS метрик)
  - [x] Исправлен ResponseRelevancy (был AnswerRelevancy)
  - [x] Причесан env.example с подробными комментариями

**Как протестировать:**
- Пройти полный цикл для каждой комбинации:
  - OpenAI + semantic
  - OpenAI + hybrid
  - OpenAI + hybrid_reranker
  - HuggingFace + semantic
  - HuggingFace + hybrid
  - HuggingFace + hybrid_reranker
- Запустить evaluation для сравнения метрик
- Проверить все edge cases
- Убедиться что README актуален

---

## 🚀 Спринт 5: ReAct Agent

### Итерация 16: Инструмент rag_search и упрощение RAG

**Цель:** Создать инструмент rag_search для агента и упростить RAG модуль

- [x] Создать `src/tools.py` с инструментом `rag_search`
- [x] Инструмент использует декоратор `@tool` из langchain_core.tools
- [x] Описание инструмента и аргументов для LLM
- [x] Возвращает форматированный контекст из найденных документов
- [x] Упростить `src/rag.py`: убрать query_transformation_chain
- [x] Добавить `retrieve_documents(query)` - базовая функция поиска
- [x] Добавить `get_retriever()` - доступ к текущему retriever
- [x] Сохранить поддержку всех 3 режимов retrieval

**Как протестировать:**
- Импортировать rag_search и вызвать напрямую
- Проверить работу со всеми режимами retrieval
- Убедиться что retrieve_documents возвращает документы

---

### Итерация 17: Создание агента и интеграция

**Цель:** Создать ReAct агента и интегрировать с текущим ботом

- [x] Создать `src/agent.py` с функцией `create_bank_agent()`
- [x] Использовать StateGraph из langgraph для создания ReAct агента
- [x] Системный промпт: универсальный банковский ассистент
- [x] Инструкции агенту: когда вызывать rag_search
- [x] Добавить MemorySaver для хранения состояния
- [x] Обновить `src/config.py`: убрать неиспользуемые настройки query_transform
- [x] Обновить `src/bot.py`: инициализировать агента
- [x] Обновить `src/handlers.py`: вызывать агента вместо rag_answer
- [x] Сохранить совместимость: агент возвращает answer и documents
- [x] Удалить неиспользуемый промпт query_transform.txt
- [x] Добавить зависимости: langgraph, langgraph-checkpoint

**Как протестировать:**
- Запустить бота и задать простой вопрос
- Проверить что агент вызывает rag_search
- Проверить что история сохраняется
- Проверить `/index_status` работает

---

### Итерация 18: Тестирование, evaluation и финальная полировка

**Цель:** Полное тестирование и обновление документации

- [x] Проверить работу evaluation с агентом
- [x] Убедиться что RAGAS метрики работают корректно
- [x] Тестирование всех режимов retrieval через агента
- [x] Проверить отображение источников (SHOW_SOURCES)
- [x] Обновить `docs/idea.md` - описать агентскую архитектуру
- [x] Обновить `docs/vision.md` - новая архитектура, поток данных
- [x] Обновить `README.md` - описание ReAct агента
- [x] Обновить `/help` команду - объяснить работу агента
- [x] Финальное тестирование всех сценариев
- [x] Проверить работу команд: /start, /help, /index, /index_status, /evaluate_dataset

**Как протестировать:**
- Пройти все сценарии из README
- Запустить `/evaluate_dataset`
- Проверить все режимы retrieval
- Убедиться что документация актуальна

---

## 🚀 Спринт 6: MCP интеграция для динамических данных

### Итерация 19: Создание MCP сервера mcp-bank-agent

**Цель:** Создать MCP сервер с инструментами search_products и currency_converter

- [x] Создать структуру проекта mcp/mcp-bank-agent
  - [x] Создать папки: mcp/mcp-bank-agent/data
  - [x] Создать pyproject.toml с зависимостями (mcp, requests)
  - [x] Создать Makefile с командой run
  - [x] Создать README.md с описанием инструментов
- [x] Собрать информацию о текущих продуктах с сайта Сбербанка (https://www.sberbank.ru/)
  - [x] Вклады, кредиты, дебетовые/кредитные карты, счета (14 продуктов)
  - [x] Создать data/bank_products.json со структурой согласно плану
- [x] Создать server.py с FastMCP
  - [x] Импорты и настройка логирования
  - [x] Функция load_products() для загрузки JSON
  - [x] Реализовать инструмент search_products
    - [x] Параметры: product_type, keyword, min/max_amount, min/max_rate, currency
    - [x] Фильтрация по параметрам через list comprehension
    - [x] Форматирование результатов (топ 10)
  - [x] Реализовать инструмент currency_converter
    - [x] Параметры: from_currency, to_currency, amount
    - [x] GET запрос к https://www.cbr-xml-daily.ru/latest.js
    - [x] Парсинг JSON и извлечение курса
    - [x] Конвертация суммы если amount указан (универсальная логика через рубли)
    - [x] Форматирование результата
  - [x] Запуск FastMCP на порту 8000 с transport="streamable-http"

**Как протестировать:**
- Запустить `cd mcp/mcp-bank-agent && make run`
- Проверить что сервер стартует на порту 8000
- Проверить статус: `make info`
- Протестировать инструменты напрямую через curl или MultiServerMCPClient

---

### Итерация 20: Интеграция MCP инструментов с агентом

**Цель:** Подключить MCP инструменты к банковскому агенту

- [x] Обновить зависимости в главном pyproject.toml
  - [x] Добавить langchain-mcp-adapters
- [x] Обновить src/agent.py
  - [x] Импортировать MultiServerMCPClient
  - [x] Создать mcp_client с конфигурацией для mcp-bank-agent
  - [x] Получить MCP инструменты через await client.list_tools()
  - [x] Объединить tools = [rag_search] + mcp_tools
  - [x] Передать все инструменты в create_agent()
  - [x] Сделать create_bank_agent() и initialize_agent() асинхронными
  - [x] Обновить bot.py для асинхронного вызова initialize_agent()
- [x] Обновить prompts/agent_system.txt
  - [x] Добавить описание search_products в секцию ИНСТРУМЕНТЫ
  - [x] Добавить описание currency_converter в секцию ИНСТРУМЕНТЫ
  - [x] Добавить секцию КОГДА ИСПОЛЬЗОВАТЬ search_products
  - [x] Добавить секцию КОГДА ИСПОЛЬЗОВАТЬ currency_converter
  - [x] Уточнить разницу между rag_search и search_products
- [x] Обновить главный Makefile (сделано в Итерации 19)
  - [x] Добавить команду run-mcp-bank для запуска MCP сервера

**Как протестировать:**
- Запустить MCP сервер в отдельном терминале: `make run-mcp-bank`
- Запустить бота: `make run`
- Проверить что агент успешно подключается к MCP серверу
- Проверить в логах что MCP инструменты загружены

---

### Итерация 21: Тестирование и финальная полировка

**Цель:** Полное end-to-end тестирование и обновление документации

- [x] Тестирование всех трех инструментов
  - [x] Диалог с использованием rag_search (общие условия из PDF)
  - [x] Диалог с использованием search_products (актуальные ставки)
  - [x] Диалог с использованием currency_converter (курсы валют)
  - [x] Комбинированные диалоги (агент сам выбирает инструмент)
- [x] Проверка edge cases
  - [x] Недоступность MCP сервера (graceful degradation - работает только rag_search)
  - [x] Асинхронные MCP инструменты через astream()
- [x] Обновить README.md
  - [x] Описать новые возможности с MCP (3 инструмента)
  - [x] Добавить инструкцию по запуску MCP сервера
  - [x] Примеры диалогов с новыми инструментами
  - [x] Обновить раздел "Архитектура"
  - [x] Обновить технологический стек
  - [x] Добавить раздел про MCP сервер
- [x] Обновить /help команду бота
  - [x] Добавить описание 3 инструментов
  - [x] Примеры вопросов для search_products
  - [x] Примеры вопросов для currency_converter
  - [x] Примеры вопросов для rag_search

**Как протестировать:**
- Запустить полный стек: MCP сервер + бот
- Провести тестовые диалоги из обновленного README
- Убедиться что все работает стабильно
- Проверить логи на отсутствие ошибок

---

## 🚀 Спринт 7: Расчет доходности вклада

### Итерация 22: Разработка инструмента deposit_income_calculator

**Цель:** Реализовать функции расчета и MCP tool для расчета доходности вкладов

- [x] Добавить функцию `calculate_simple_interest()` в server.py
- [x] Добавить функцию `calculate_compound_interest()` с капитализацией в server.py
- [x] Добавить функцию `calculate_tax()` для расчета НДФЛ в server.py
- [x] Добавить функцию `format_deposit_calculation()` для форматирования результата
- [x] Создать MCP tool `deposit_income_calculator` с 7 параметрами
- [x] Протестировать локально все функции расчета

**Как протестировать:**
- Запустить MCP сервер: `cd mcp/mcp-bank-agent && make run`
- Проверить что сервер стартует без ошибок
- Проверить логи что deposit_income_calculator зарегистрирован

---

### Итерация 23: Интеграция с агентом и обновление промптов

**Цель:** Обновить системный промпт агента с детальными инструкциями по уточнению параметров

- [x] Обновить `prompts/agent_system.txt`:
  - [x] Добавить описание deposit_income_calculator в секцию ИНСТРУМЕНТЫ
  - [x] Добавить секцию ПРАВИЛА ИСПОЛЬЗОВАНИЯ с примерами правильного/неправильного поведения
  - [x] Добавить примеры уточняющих вопросов
  - [x] Добавить секцию КОГДА ИСПОЛЬЗОВАТЬ deposit_income_calculator
- [x] Перезапустить бота и проверить что новый инструмент загружен
- [x] Проверить в логах список всех 4 инструментов (rag_search + 3 MCP)

**Как протестировать:**
- Запустить MCP сервер + бота
- Проверить логи при старте: должно быть 4 инструмента
- Проверить что deposit_income_calculator появился в списке

---

### Итерация 24: Документация и тестирование

**Цель:** Обновить документацию и протестировать все сценарии использования

- [x] Обновить `docs/idea.md`:
  - [x] Добавить deposit_income_calculator в секцию "Этап 5: MCP интеграция"
  - [x] Добавить пример диалога с уточнением параметров
- [x] Обновить `docs/vision.md`:
  - [x] Добавить deposit_income_calculator в секцию "9. mcp/mcp-bank-agent/"
  - [x] Обновить поток данных с третьим MCP инструментом
- [x] Обновить `README.md`:
  - [x] Добавить deposit_income_calculator в список MCP инструментов
  - [x] Добавить примеры использования
- [x] Обновить команду `/help` в `src/handlers.py`:
  - [x] Добавить примеры вопросов для deposit_income_calculator
- [x] Обновить `mcp/mcp-bank-agent/README.md`:
  - [x] Добавить описание третьего инструмента с параметрами
- [x] Тестирование всех сценариев:
  - [x] Уточнение недостающих параметров (нет суммы/ставки/срока)
  - [x] Простой процент без налогов
  - [x] Сложный процент с капитализацией
  - [x] Расчет с учетом налогов
  - [x] Детализированный отчет с разбивкой
  - [x] Комбинированные диалоги (поиск вклада → расчет дохода)

**Как протестировать:**
- Задать вопрос без параметров: "Посчитай доход с вклада"
  → Агент должен уточнить сумму, ставку, срок
- После уточнения → Агент вызывает deposit_income_calculator
- Проверить все комбинации параметров
- Убедиться что документация актуальна

---

## 🚀 Спринт 8: Human-in-the-Loop для критичных операций

### Итерация 25: MCP инструмент open_credit_card

**Цель:** Добавить инструмент для открытия кредитных/дебетовых карт в MCP сервер

- [x] Добавить константу `MOCK_CARD_NUMBER = "4274 3000 1234 5678"`
- [x] Создать MCP tool `open_credit_card` с параметрами card_type, client_name
- [x] Вернуть JSON с данными карты (номер, платежная система, expiration_date, client_name)
- [x] НЕ возвращать CVV для безопасности
- [x] Определять платежную систему по первой цифре (4=Visa, 5=Mastercard)
- [x] Добавить логирование операции
- [x] Протестировать через MCP client

**Как протестировать:**
- Запустить MCP сервер: `cd mcp/mcp-bank-agent && make run`
- Проверить что сервер стартует без ошибок
- Проверить в логах регистрацию нового инструмента

---

### Итерация 26: Интеграция HITL в агента

**Цель:** Добавить Human-in-the-Loop middleware для критичных операций

- [x] Импортировать `HumanInTheLoopMiddleware` из langchain.agents.middleware
- [x] Импортировать `Command` из langgraph.types
- [x] Добавить middleware в `create_bank_agent()` с настройкой для open_credit_card
- [x] Переработать `agent_answer()` на astream с обработкой `__interrupt__` ключа
- [x] Вернуть interrupt объект если требуется подтверждение
- [x] Добавить функцию `agent_resume()` для продолжения после approve/reject
- [x] Добавить `_run_agent_stream()` для избежания дублирования кода
- [x] Обновить `prompts/agent_system.txt` с описанием open_credit_card

**Как протестировать:**
- Запустить бота и MCP сервер
- Попросить открыть карту
- Проверить что появляется interrupt (в логах)

---

### Итерация 27: UI и обработка HITL в боте

**Цель:** Создать пользовательский интерфейс для подтверждения операций

- [x] Обновить `src/handlers.py`:
  - [x] Добавить импорты InlineKeyboardMarkup, InlineKeyboardButton, CallbackQuery
  - [x] Добавить глобальный словарь `pending_interrupts: dict[int, object]`
  - [x] Обработать interrupt в handle_message: показать кнопки
  - [x] Добавить обработчик `@router.callback_query` для кнопок
  - [x] Вызвать `agent.agent_resume()` с решением пользователя
  - [x] Удалить кнопки после нажатия через `edit_reply_markup(reply_markup=None)`
  - [x] Обработка ошибок с try/except
- [x] Обновить команду `/help` с примерами использования open_credit_card

**Как протестировать:**
- Запросить открытие карты
- Проверить появление кнопок Accept/Reject под сообщением
- Нажать Accept - проверить выполнение операции
- Повторить с Reject - проверить отклонение

---

### Итерация 28: Тестирование и документация

**Цель:** Полное тестирование и обновление документации

- [x] Сквозное тестирование approve сценария
- [x] Сквозное тестирование reject сценария  
- [x] Проверить корректность работы с несколькими пользователями
- [x] Обновить `docs/idea.md` - добавить Этап 6 с HITL
- [x] Обновить `docs/vision.md` - описать архитектуру HITL
- [x] Обновить `README.md` - добавить примеры с подтверждением
- [x] Проверить что все зависимости установлены

**Как протестировать:**
- Пройти полный сценарий использования
- Убедиться что документация актуальна
- Проверить универсальность решения (легко ли добавить новый инструмент с HITL)

---

## 🚀 Спринт 9: PII Protection (маскирование чувствительных данных)

### Итерация 29: Модуль `middleware.py` и `PIIMiddleware`

**Цель:** Маскировать номера банковских карт в тексте ответов модели (видимый вывод в Telegram).

- [x] Создать `src/middleware.py`: словарь `PATTERNS`, класс `PIIMiddleware` на базе `AgentMiddleware`
- [x] Реализовать маскирование через `wrap_model_call` / `awrap_model_call` (только `AIMessage`, `apply_to_output=True`)
- [x] Формат маски: `****-****-****-<последние 4 цифры>`

**Как протестировать:**

- Юнит-проверка: `_mask_model_result` / ручной вызов с `AIMessage` содержащим `4274 3000 1234 5678` → в контенте только маска для последней группы
- После интеграции в агента: сценарий `open_credit_card` → approve → в ответе бота нет полного PAN

---

### Итерация 30: Интеграция в агента

**Цель:** Подключить PII как первый слой в `middleware=[...]` у `create_agent()`.

- [x] Импорт `PIIMiddleware` в `src/agent.py`
- [x] Порядок: `PIIMiddleware(...)`, затем `HumanInTheLoopMiddleware(...)`

**Как протестировать:**

- Локально: `approve` после запроса карты — текст ответа без полного номера
- Убедиться, что вход пользователя без маски (виден агенту как раньше)

---

### Итерация 31: Документация

**Цель:** Зафиксировать требования и место расширения в docs и правилах.

- [x] Обновить `docs/idea.md` — Этап 7 (PII)
- [x] Обновить `docs/vision.md` — структура, компонент `agent.py`, шаг в потоке данных
- [x] Обновить `.cursor/rules/conventions.mdc` — модуль 09-security, пункт PII
- [x] Обновить `README.md` — кратко про маскирование карт

**Как протестировать:**

- Просмотреть согласованность ссылок `middleware.py` / порядок middleware

---

### Итерация 32: Сквозное тестирование

**Цель:** Убедиться, что PII-маскирование работает end-to-end через реального бота.

- [x] `open_credit_card` → approve → PAN в ответе замаскирован (`****-****-****-XXXX`) ✅
- [x] `open_credit_card` → reject → ответ об отклонении без утечки PAN ✅
- [x] Обычные запросы (курс валюты, условия кредита) — middleware не ломает обычные ответы ✅
- [x] `/start` → повторный запрос карты — HITL-кнопки не зависают от предыдущей сессии ✅

---

## 🚀 Спринт 10: Call Limits (лимиты вызовов LLM и инструментов)

### Итерация 33: Классы в `middleware.py`

**Цель:** За один run агента ограничить число обращений к модели и к инструментам.

- [x] `ModelCallLimitMiddleware` — счётчик в `wrap_model_call` / `awrap_model_call`, при превышении → `AIMessage` с понятным текстом
- [x] `ToolCallLimitMiddleware` — счётчик в `wrap_tool_call` / `awrap_tool_call`, при превышении → `ToolMessage` с пояснением
- [x] Счётчик **без** `before/after_agent`: LangGraph запускает каждый нод через `copy_context()` — ContextVar/asyncio.current_task не пробрасываются между нодами (bugfix #3)
  - `ModelCallLimitMiddleware`: считает `AIMessage` в `request.state['messages']` → порядковый номер вызова
  - `ToolCallLimitMiddleware`: считает `ToolMessage` в messages (прошлые батчи) + per-batch счётчик по `id(request.state)` (все параллельные вызовы одного батча получают один и тот же объект state)

---

### Итерация 34: Подключение в агенте

**Цель:** Порядок middleware как в vision: лимиты → PII → HITL.

- [x] `create_agent(..., middleware=[ModelCallLimitMiddleware(3,...), ToolCallLimitMiddleware(3,...), PIIMiddleware(...), HumanInTheLoopMiddleware(...)])`

---

### Итерация 35: Документация

**Цель:** Зафиксировать требования в idea/vision/conventions/README.

- [x] Обновить `docs/idea.md` — Этап 8
- [x] Обновить `docs/vision.md` — компонент `agent.py`, сценарий 7, поток данных, структура
- [x] Обновить `.cursor/rules/conventions.mdc` и `README.md` (лимиты)

---

### Итерация 36: Сквозное тестирование

**Цель:** Убедиться, что типовые сценарии не ломаются, а искусственно «длинный» цикл утыкается в лимит.

- [x] Простой вопрос (вклад под 15.5%) — 1 tool + 2 model calls, лимиты не сработали ✓
- [x] Запрос с 4 параллельными инструментами — 3 прошли, 4-й заблокирован (`total=4 run_limit=3`) ✓
- [x] Следующее сообщение пользователя — счётчики сброшены, агент отвечает нормально ✓

---

---

## 🚀 Спринт 11: Открытие вклада с подтверждением (HITL)

### Итерация 37: MCP инструмент `open_deposit`

**Цель:** Добавить новый инструмент открытия вклада в MCP сервер банковского агента.

- [x] Добавить `open_deposit(client_name, amount, term_months, rate)` в `mcp/mcp-bank-agent/server.py`
- [x] Мок-реализация: генерация номера договора (`ВКЛ-YYYYMMDD-XXXXXX`), расчёт дохода через `calculate_simple_interest`
- [x] Возвращает: номер договора, детали вклада, ожидаемый доход и итоговую сумму

**Как протестировать:**

- Запустить MCP сервер: `make run-mcp-bank`
- Проверить что инструмент виден: `curl http://localhost:8000/mcp/tools`
- Вызвать инструмент напрямую с параметрами и убедиться в корректном ответе

---

### Итерация 38: Интеграция HITL для `open_deposit`

**Цель:** Добавить `open_deposit` в `HumanInTheLoopMiddleware` в `src/agent.py`.

- [x] Добавить `"open_deposit": {"allowed_decisions": ["approve", "reject"]}` в `interrupt_on`
- [x] Убедиться, что порядок middleware не изменился: лимиты → PII → HITL

**Как протестировать:**

- Запросить открытие вклада → агент должен запросить отсутствующие параметры
- После получения всех параметров → агент вызывает `open_deposit` → появляются кнопки HITL
- Approve → вклад открывается, возвращается номер договора
- Reject → агент сообщает об отклонении

---

### Итерация 39: Обновление промпта и документации

**Цель:** Добавить инструкции для `open_deposit` в системный промпт и зафиксировать изменения в docs.

- [x] Обновить `prompts/agent_system.txt`: секция инструмента 6, `КОГДА ИСПОЛЬЗОВАТЬ open_deposit`, `ПРАВИЛА ИСПОЛЬЗОВАНИЯ open_deposit` с примерами
- [x] Обновить `docs/idea.md` — Этап 9: открытие вклада с HITL
- [x] Обновить `docs/vision.md` — инструменты MCP, поток данных, принципы разделения инструментов, сценарий 8

---

### Итерация 40: Сквозное тестирование

**Цель:** Убедиться, что end-to-end сценарий работает без регрессий.

- [x] `open_deposit` → параметры указаны частично → агент уточняет → HITL ✅
- [x] Approve → номер договора и расчёт дохода в ответе ✅
- [x] Reject → ответ об отклонении, вклад не открыт ✅
- [x] `open_credit_card` — HITL работает как раньше ✅
- [x] Исправлен баг: `callback.answer()` перенесён в начало обработчика (Telegram 30-сек таймаут) ✅
- [x] Исправлен баг: `edit_text` без `parse_mode` — спецсимволы в параметрах не ломают Markdown ✅
- [x] Добавлен fallback для `currency_converter`: `cbr-xml-daily.ru` → `open.er-api.com` ✅

---

## 📝 Заметки

После завершения каждой итерации:
1. Обновить статус в таблице прогресса
2. Протестировать бота
3. Закоммитить изменения
4. Переходить к следующей итерации только после успешного теста

